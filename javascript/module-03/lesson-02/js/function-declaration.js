const names = ["mango", "poly", "ajax"];
let total = 0;

// Coding
printNames(names);
handleFormSubmit(total);

// Functions
function printNames() {}
function getUserData() {}
function handleFormSubmit() {
  getUserData();
}
function handleButtonClick() {}
